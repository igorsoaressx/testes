package model; // Mantendo o seu pacote

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name = "clientes")
public class Cliente extends Usuario {
    
    // A CLASSE DEVE FICAR COMPLETAMENTE VAZIA AQUI DENTRO.
    // ELA JÁ HERDA TUDO O QUE PRECISA DA CLASSE "Usuario".
    
}